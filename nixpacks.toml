# Ultra-minimal Bagisto nixpacks.toml - Skip frontend build

[phases.setup]
nixPkgs = ["php82Packages.composer"]

[phases.install]
cmds = [
  "composer install --optimize-autoloader --no-dev --no-interaction"
]

[start]
cmd = 'php artisan serve --host=0.0.0.0 --port=${PORT:-8000}'
